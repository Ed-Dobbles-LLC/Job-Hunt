[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd .mastra/output && node index.mjs"
healthcheckPath = "/api/dashboard"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "job-match"
internalPort = 5000
