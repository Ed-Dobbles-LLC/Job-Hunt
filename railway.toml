[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node /app/.mastra/output/index.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "job-match"
internalPort = 5000
